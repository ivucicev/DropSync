# Self-hosted Coturn TURN server (required for 5G/CGNAT connections)
# Without these, mobile users on carrier-grade NAT can't connect.
#
# Set up Coturn on any VPS, then fill these in and rename to .env
VITE_TURN_URL=turn.yourdomain.com
VITE_TURN_USERNAME=dropsync
VITE_TURN_CREDENTIAL=your_turn_password
